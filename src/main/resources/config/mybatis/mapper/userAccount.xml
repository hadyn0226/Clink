<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<!-- SQL문들을 작성하는 파일 -->
	<insert id="insert" parameterType="uservo">
		insert into useraccount 
		(user_id, user_firstname, user_lastname, user_email, user_password,user_salt, user_date, user_birth)
		 values(user_seq.nextval, #{userFirstname}, #{userLastname}, #{email}, #{userPassword}, #{userSalt}, sysdate, #{userBirth})
	</insert>
	
	<select id="getSalt" resultType="String" parameterType="String">
		SELECT USER_SALT FROM USERACCOUNT WHERE USER_EMAIL = #{email}
	</select>
	
	<select id="login" resultType="uservo" parameterType="uservo">
		SELECT * FROM USERACCOUNT WHERE USER_EMAIL = #{email} AND USER_PASSWORD = #{userPassword}
	</select>

</mapper>












